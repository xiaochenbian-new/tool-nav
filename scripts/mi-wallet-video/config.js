/**
 * 小米钱包 · 看视频领奖励 — 默认配置
 * 可在工具页生成脚本时覆盖，或直接在手机上修改本文件。
 */
module.exports = {
    /** 小米钱包包名 */
    packageName: "com.mipay.wallet",

    /** 单次运行最多完成几轮视频任务（0 = 不限制，直到找不到任务） */
    maxRounds: 10,

    /** 进入视频后等待秒数（广告/短视频播放时长，按实际调整） */
    videoWaitSec: 18,

    /** 每步操作间隔（毫秒） */
    stepDelayMs: 1200,

    /** 找不到控件时的重试次数 */
    maxRetries: 3,

    /** 启动 App 后等待首页加载（毫秒） */
    launchWaitMs: 3500,

    /** 任务入口：按顺序尝试点击的文案（正则字符串，不含斜杠） */
    taskEntryTexts: [
        "任务中心",
        "赚积分",
        "领奖励",
        "每日任务",
        "福利",
        "活动"
    ],

    /** 开始看视频按钮文案 */
    startVideoTexts: [
        "去看视频",
        "观看视频",
        "浏览视频",
        "立即观看",
        "去完成",
        "去观看"
    ],

    /** 领取奖励按钮文案 */
    claimTexts: [
        "立即领取",
        "领取奖励",
        "领取",
        "开心收下",
        "知道了"
    ],

    /** 关闭弹窗 / 返回 */
    dismissTexts: [
        "关闭",
        "跳过",
        "取消",
        "下次再说",
        "×"
    ],

    /** 判定任务已完成的文案（出现则跳过当前轮） */
    doneTexts: [
        "已完成",
        "明日再来",
        "今日已领",
        "已达上限"
    ],

    /** 是否显示悬浮日志窗 */
    showFloaty: true,

    /** 调试模式：打印当前界面可点击控件 */
    debug: false
};
